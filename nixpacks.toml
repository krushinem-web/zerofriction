[phases.setup]
nixPkgs = ["nodejs", "pnpm-9_x"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["echo 'Build skipped - pnpm install handles everything'"]

[start]
cmd = "node server.js"
