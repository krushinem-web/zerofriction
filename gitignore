# Package manager - use ONLY pnpm-lock.yaml
package-lock.json
npm-debug.log*
yarn.lock

# Node modules
node_modules/

# Environment variables
.env
.env.local
.env.*.local

# Railway specific
.railway/

# Logs
logs/
*.log

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary files
tmp/
temp/
*.tmp
```

#### FILE 2: `.npmrc`
Create `.npmrc` in repository root:
```
# Force clean installs (no cache)
cache=false

# Prevent package-lock.json generation
package-lock=false

# Use exact versions
save-exact=true

# Strict engine validation
engine-strict=true

# Disable progress bar for cleaner CI logs
progress=false

# Fail on any warnings during install
strict-ssl=true
